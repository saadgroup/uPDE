[pytest]
# Default flags: verbose output, short tracebacks, stop on first failure.
# Override on the command line as needed, e.g.:
#   pytest -x          stop after first failure
#   pytest --rebless   regenerate golden snapshots
#   pytest -k reg      run only regression tests
addopts = -v --tb=short
testpaths = tests
